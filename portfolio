import React from "react";
import { motion } from "framer-motion";
import { Github, Linkedin, Mail, Phone, MapPin, GraduationCap, Award, Briefcase, ExternalLink, Download } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

// --- Brand palette inspired by Netflix ---
const brand = {
  bg: "#141414",
  panel: "#181818",
  red: "#E50914",
  gray: "#A3A3A3",
  white: "#FFFFFF",
};

const data = {
  name: "Vinay Ramachandramurthy",
  title: "Business Analyst • Data & Operations",
  location: "Springfield, IL, USA",
  phone: "+1 (507) 783-4092",
  email: "vinayramachandramurthy@gmail.com",
  linkedin: "https://www.linkedin.com/in/vinay-ramachandramurthy",
  summary:
    "Business Analyst with hands-on experience streamlining reporting, automating workflows, and building KPI dashboards. Comfortable translating complex data into clear decisions across public-sector and corporate environments.",
  skills: [
    "Excel (Power Query, Pivots, VBA)",
    "SQL (PostgreSQL, MySQL)",
    "Tableau",
    "Power BI",
    "Smartsheet",
    "SharePoint",
    "JIRA",
    "Python (pandas)",
  ],
  certs: [
    { name: "PMI – CAPM", org: "Project Management Institute" },
    { name: "Google Project Management", org: "Coursera" },
    { name: "Google AI Essentials", org: "Coursera" },
  ],
  education: [
    {
      school: "University of Illinois Springfield",
      degree: "M.S. – Management Information Systems (GPA 3.83/4.0)",
      dates: "Jan 2024 – May 2025",
      icon: "UIS",
    },
    {
      school: "Nitte Meenakshi Institute of Technology",
      degree: "B.E. – Electronics & Communications (GPA 3.1/4.0)",
      dates: "Aug 2017 – Jul 2021",
      icon: "NMIT",
    },
  ],
  experience: [
    {
      company: "Illinois Department of Commerce & Economic Opportunity",
      role: "Business Analyst Intern (Graduate Public Service Intern)",
      dates: "Sep 2024 – May 2025",
      bullets: [
        "Streamlined reporting workflows; cut monthly prep time by ~30% via Smartsheet + Excel automation.",
        "Built executive KPI dashboards to improve transparency and decision speed.",
        "Coordinated inter‑agency stakeholders; ensured compliance and on‑time delivery.",
        "Organized SharePoint repositories for audit‑ready documentation.",
      ],
    },
    {
      company: "Accenture (LinkedIn Client)",
      role: "Business Analyst",
      dates: "Nov 2022 – Dec 2023",
      bullets: [
        "Developed real‑time Tableau monitoring dashboards for operational metrics.",
        "Resolved 90% of high‑priority escalations within 48 hours using RCA.",
        "Defined business/functional requirements with Product & Engineering; authored SOPs and training.",
      ],
    },
    {
      company: "Mindtree Ltd.",
      role: "Associate Software Engineer",
      dates: "Apr 2022 – Oct 2022",
      bullets: [
        "Supported Agile sprints, backlog grooming, and release notes.",
        "Triaged defects in JIRA; improved resolution time by ~20%.",
      ],
    },
  ],
  projects: [
    {
      name: "Inventory Management System (SQL + AWS)",
      link: "#",
      points: [
        "Reduced stock discrepancies ~15% via automated alerts and validations.",
        "Power BI drill‑downs for data‑driven decisions; EC2/S3 for scalable reporting.",
      ],
    },
  ],
};

// Simple poster block with gradient placeholder
const Poster = ({ label }: { label: string }) => (
  <div className="aspect-[16/9] w-full rounded-2xl bg-gradient-to-br from-zinc-800 to-zinc-600 grid place-items-center text-zinc-200 text-sm font-medium">
    {label}
  </div>
);

const SectionHeader = ({ title, action }: { title: string; action?: React.ReactNode }) => (
  <div className="mb-4 flex items-end justify-between">
    <h2 className="text-xl md:text-2xl font-semibold text-white tracking-tight">{title}</h2>
    {action}
  </div>
);

const Pill = ({ children }: { children: React.ReactNode }) => (
  <span className="rounded-full border border-zinc-700 bg-zinc-800/60 px-3 py-1 text-xs text-zinc-200">{children}</span>
);

export default function PortfolioNetflix() {
  return (
    <div className="min-h-screen" style={{ backgroundColor: brand.bg }}>
      {/* Top Nav */}
      <header className="sticky top-0 z-50 border-b border-zinc-800/70" style={{ backgroundColor: brand.bg }}>
        <div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3">
          <div className="flex items-center gap-2">
            <div className="h-6 w-6 rounded-sm" style={{ backgroundColor: brand.red }} />
            <span className="text-white text-lg font-semibold tracking-wide">VINAYFLIX</span>
          </div>
          <nav className="hidden md:flex items-center gap-6 text-sm">
            {[
              ["Home", "#top"],
              ["Experience", "#experience"],
              ["Projects", "#projects"],
              ["Skills", "#skills"],
              ["Education", "#education"],
              ["Contact", "#contact"],
            ].map(([label, href]) => (
              <a key={label} href={href as string} className="text-zinc-300 hover:text-white transition-colors">
                {label}
              </a>
            ))}
          </nav>
          <div className="flex items-center gap-2">
            <a href={data.linkedin} target="_blank" rel="noreferrer">
              <Button size="sm" className="bg-transparent hover:bg-zinc-800 text-zinc-200 border border-zinc-700">
                <Linkedin className="mr-2 h-4 w-4" /> LinkedIn
              </Button>
            </a>
            <a href="mailto:vinayramachandramurthy@gmail.com">
              <Button size="sm" style={{ backgroundColor: brand.red }} className="hover:opacity-90">
                <Mail className="mr-2 h-4 w-4" /> Contact
              </Button>
            </a>
          </div>
        </div>
      </header>

      {/* Hero */}
      <section id="top" className="mx-auto max-w-7xl px-4 pt-10 pb-6">
        <div className="grid gap-6 md:grid-cols-[2fr,1fr] items-stretch">
          <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.5 }}>
            <div className="rounded-2xl p-6" style={{ backgroundColor: brand.panel }}>
              <div className="mb-4 flex flex-wrap items-center gap-2">
                <Pill>Business Analyst</Pill>
                <Pill>Data & Ops</Pill>
                <Pill>Open to Relocate</Pill>
              </div>
              <h1 className="text-3xl md:text-4xl font-bold text-white">{data.name}</h1>
              <p className="mt-2 text-zinc-300 max-w-2xl">{data.summary}</p>
              <div className="mt-4 flex flex-wrap gap-4 text-zinc-300">
                <span className="inline-flex items-center gap-2"><MapPin className="h-4 w-4" /> {data.location}</span>
                <a className="inline-flex items-center gap-2 hover:text-white" href="tel:+15077834092"><Phone className="h-4 w-4" /> {data.phone}</a>
                <a className="inline-flex items-center gap-2 hover:text-white" href={`mailto:${data.email}`}><Mail className="h-4 w-4" /> {data.email}</a>
              </div>
              <div className="mt-6 flex flex-wrap gap-2">
                {data.skills.slice(0, 6).map((s) => (
                  <Pill key={s}>{s}</Pill>
                ))}
              </div>
              <div className="mt-6 flex gap-2">
                <a href="#experience"><Button className="bg-zinc-100 text-black hover:bg-white">View Experience</Button></a>
                <a href="#projects"><Button className="bg-transparent border border-zinc-600 text-zinc-200 hover:bg-zinc-800">Projects</Button></a>
              </div>
            </div>
          </motion.div>
          <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.6 }}>
            <div className="rounded-2xl p-4 md:p-6 h-full" style={{ backgroundColor: brand.panel }}>
              <SectionHeader title="Now Streaming: Highlights" />
              <div className="grid gap-3">
                <Poster label="30% faster reporting (Smartsheet + Excel)" />
                <Poster label="Dashboards execs actually use (Tableau/Power BI)" />
              </div>
            </div>
          </motion.div>
        </div>
      </section>

      {/* Experience */}
      <section id="experience" className="mx-auto max-w-7xl px-4 py-8">
        <SectionHeader title="Experience" />
        <div className="grid gap-4 md:grid-cols-3">
          {data.experience.map((exp, i) => (
            <motion.div key={exp.company} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: i * 0.05 }}>
              <Card className="border-zinc-800 bg-zinc-900/60 backdrop-blur">
                <CardContent className="p-5">
                  <div className="mb-2 flex items-center justify-between">
                    <h3 className="text-white font-semibold">{exp.company}</h3>
                    <span className="text-xs text-zinc-400">{exp.dates}</span>
                  </div>
                  <p className="text-zinc-300 text-sm mb-3">{exp.role}</p>
                  <ul className="list-disc pl-4 space-y-1 text-zinc-300 text-sm">
                    {exp.bullets.map((b) => (
                      <li key={b}>{b}</li>
                    ))}
                  </ul>
                </CardContent>
              </Card>
            </motion.div>
          ))}
        </div>
      </section>

      {/* Projects */}
      <section id="projects" className="mx-auto max-w-7xl px-4 py-8">
        <SectionHeader title="Projects" />
        <div className="grid gap-4 md:grid-cols-2">
          {data.projects.map((p) => (
            <Card key={p.name} className="border-zinc-800 bg-zinc-900/60">
              <CardContent className="p-5">
                <div className="mb-3">
                  <h3 className="text-white font-semibold flex items-center gap-2">
                    {p.name}
                    <a href={p.link} className="text-zinc-300 hover:text-white" target="_blank" rel="noreferrer">
                      <ExternalLink className="h-4 w-4" />
                    </a>
                  </h3>
                </div>
                <Poster label="Power BI • SQL • AWS EC2/S3" />
                <ul className="mt-3 list-disc pl-5 text-zinc-300 text-sm space-y-1">
                  {p.points.map((pt) => (
                    <li key={pt}>{pt}</li>
                  ))}
                </ul>
              </CardContent>
            </Card>
          ))}
        </div>
      </section>

      {/* Skills & Certs */}
      <section id="skills" className="mx-auto max-w-7xl px-4 py-8">
        <div className="grid gap-6 md:grid-cols-2">
          <div className="rounded-2xl p-6" style={{ backgroundColor: brand.panel }}>
            <SectionHeader title="Skills" />
            <div className="flex flex-wrap gap-2">
              {data.skills.map((s) => (
                <Pill key={s}>{s}</Pill>
              ))}
            </div>
          </div>
          <div className="rounded-2xl p-6" style={{ backgroundColor: brand.panel }}>
            <SectionHeader title="Certifications" />
            <ul className="space-y-2">
              {data.certs.map((c) => (
                <li key={c.name} className="flex items-center gap-3 text-zinc-200">
                  <Award className="h-4 w-4 text-red-600" />
                  <span className="font-medium">{c.name}</span>
                  <span className="text-zinc-400 text-sm">• {c.org}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </section>

      {/* Education */}
      <section id="education" className="mx-auto max-w-7xl px-4 py-8">
        <SectionHeader title="Education" />
        <div className="grid gap-4 md:grid-cols-2">
          {data.education.map((e) => (
            <Card key={e.school} className="border-zinc-800 bg-zinc-900/60">
              <CardContent className="p-5 flex items-center gap-4">
                <div className="h-12 w-12 rounded bg-zinc-800 grid place-items-center text-white text-xs font-semibold">
                  {e.icon}
                </div>
                <div>
                  <div className="text-white font-medium">{e.school}</div>
                  <div className="text-zinc-300 text-sm">{e.degree}</div>
                  <div className="text-zinc-500 text-xs">{e.dates}</div>
                </div>
              </CardContent>
            </Card>
          ))}
        </div>
      </section>

      {/* Contact */}
      <section id="contact" className="mx-auto max-w-7xl px-4 py-10">
        <div className="rounded-2xl p-6 md:p-8" style={{ backgroundColor: brand.panel }}>
          <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
            <div>
              <h3 className="text-white text-2xl font-semibold">Let’s connect</h3>
              <p className="text-zinc-300 mt-1 max-w-2xl">
                Want the full résumé or a PDF export? Ping me and I’ll send it over. Happy to chat about Business Analyst, Ops, or Data roles.
              </p>
              <div className="mt-3 flex flex-wrap gap-3 text-zinc-200">
                <a className="inline-flex items-center gap-2 hover:text-white" href={`mailto:${data.email}`}>
                  <Mail className="h-4 w-4" /> {data.email}
                </a>
                <a className="inline-flex items-center gap-2 hover:text-white" href="tel:+15077834092">
                  <Phone className="h-4 w-4" /> {data.phone}
                </a>
                <a className="inline-flex items-center gap-2 hover:text-white" href={data.linkedin} target="_blank" rel="noreferrer">
                  <Linkedin className="h-4 w-4" /> LinkedIn
                </a>
              </div>
            </div>
            <div className="flex gap-2">
              <Button className="bg-transparent border border-zinc-600 text-zinc-200 hover:bg-zinc-800">
                <Download className="mr-2 h-4 w-4" /> Download PDF
              </Button>
              <a href="#top"><Button style={{ backgroundColor: brand.red }} className="hover:opacity-90">Back to Top</Button></a>
            </div>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="pb-10 pt-6">
        <div className="mx-auto max-w-7xl px-4 text-center text-zinc-500 text-xs">
          © {new Date().getFullYear()} {data.name}. Built with a Netflix‑inspired UI.
        </div>
      </footer>
    </div>
  );
}
